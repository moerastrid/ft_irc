class Channel {
    private:
        Channel();
    public:
        ~Channel();
        Channel(const Channel& other);
        Channel& operator=(const Channel& other);
        // Channel(/*args go here*/);
};